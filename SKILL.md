---
name: finreg-development
description: FinReg의 금융 법규 확인 LLM을 설계·구현·검토할 때 사용한다. 프로젝트 PRD에 따라 법규 수집, 조문·버전 데이터 모델, 시점 기반 검색, 근거 답변, 자체 모델 미세조정과 평가의 기준을 적용한다. 일반 금융 상담이나 개별 사건의 법률 판단에는 사용하지 않는다.
---

# FinReg 개발 스킬

FinReg의 코드·데이터·API·모델·평가·문서를 작업할 때 이 지침을 적용한다. 제품의 목적은 대한민국 금융 법규를 찾아 근거와 적용 조건을 확인하도록 돕는 것이다. 개별 사건의 위법 여부를 최종 판정하는 기능으로 확대하지 않는다.

기준 문서는 [FinReg PRD v1.0](docs/FinReg_LLM_PRD_v1.md)이다. 아래 절 번호는 해당 PRD를 가리킨다. 작업에 관련된 원문 요구사항과 수용 기준을 먼저 읽고, 사용자에게서 확정된 변경사항이 있으면 관련 문서와 구현에 반영한다. 불일치를 발견하면 기존 결정을 임의로 바꾸지 말고 영향과 미확정 사항을 기록한다.

PRD는 제안 초안이다. 수량·성능·일정·기술 후보를 이미 달성한 결과나 확정된 법적 의무로 표현하지 않는다. 이 문서의 **추가 권장 설계**는 PRD를 구현하기 위한 제안이며, 현재 저장소에 구현되어 있다는 뜻이 아니다. 법령 내용·조문 번호·시행일·API 가용성은 실제 공식 자료를 확인하기 전까지 미확인으로 다룬다.

## 1. 작업 범위와 우선순위

- **P0:** 은행 대출상품 설명 관련 금융소비자보호 법규 확인. 질문·기준일 입력, 검색, 근거 답변, 원문 보기, 추가 질문·보류, 자체 추론, 미세조정 비교를 완성한다.
- **수집 범위:** 금융소비자 보호에 관한 법률, 관련 시행령·감독규정, 필요한 정의·위임 조항·부칙·별표. 공식 FAQ·해석 자료는 문서 성격을 구분해 보조 자료로 사용한다.
- **P1:** 개정 비교, 정기 갱신, 질문 이력, 관리자 화면, Spring Boot 연동. P0 평가 통과 후 추진한다. P0에도 관리자 수집 API와 수동 갱신은 필요하다.
- **P2:** RAFT 방식 데이터 구성, 추가 사전학습, 관계 그래프 고도화. 관찰한 오류와 개선 가설이 있을 때 검토한다.
- **초기 기술 방향:** Python, FastAPI, PostgreSQL, 자체 Transformers 추론, PyTorch·PEFT·TRL 기반 학습. BM25 계열과 임베딩 검색을 결합하고, pgvector·reranker·Gradio는 PRD의 후보 또는 선택 사항으로 다룬다.
- 장비·메모리·예산 확인 전 모델이나 처리량을 확정하지 않는다. 3B~8B는 출발 후보 범위다. 일정이 부족하면 도메인·화면·연구 기능을 줄이고 원문 검수와 평가를 유지한다.

세부 범위는 PRD 3절, 기술 선택은 8·10절을 확인한다.

## 2. 핵심 원칙

1. **주장과 근거를 연결한다.** 법률상 의무·허용·예외에 관한 핵심 주장마다 실제 확보한 근거 ID를 연결한다. 모델 내부 지식만으로 결론을 확정하지 않는다.
2. **시간의 의미를 분리한다.** 기준일·거래일·공포일·시행일·수집일을 구분한다. 최근 수집 여부와 해당 거래에 적용 가능한지는 별도로 확인한다.
3. **원문과 문맥을 보존한다.** 정규화·청킹 과정에서 부정 표현·단서·예외·단위·조건·상위 조문·참조 관계가 사라지지 않도록 한다.
4. **자료의 성격과 한계를 보존한다.** 법률·시행령·감독규정·FAQ·해석 자료를 동일한 효력으로 취급하지 않는다. 자료가 충돌하면 출처와 차이를 드러낸다.
5. **불확실성을 상태와 질문으로 표현한다.** 부족한 사실관계, 미확보 자료, 지원 범위 밖, 시스템 장애를 구분한다. 검색 실패를 규정 부재로 해석하지 않는다.
6. **인용 무결성과 의미적 정합성을 구분한다.** ID·버전·발췌·시점 검사는 필수다. 실제로 해당 조문이 주장을 뒷받침하는지는 별도 검수·평가가 필요하다.
7. **데이터 갱신과 학습을 분리한다.** 법규 개정은 검증된 말뭉치와 검색 인덱스에 반영한다. 재학습은 필요성과 평가 결과에 따라 결정한다.
8. **변경의 효과를 재현 가능한 실험으로 확인한다.** 자체 학습 실행을 성능 개선으로 간주하지 않는다. 모델·데이터·검색·프롬프트·평가 조건과 실패 사례를 남긴다.

## 3. 공통 어휘

문서·코드·API에서 다음 의미를 일관되게 사용한다. 사용자 화면은 한국어로 설명하고, 식별자에는 아래 영문 명칭을 우선한다.

| 어휘 / 식별자                                  | 의미와 구분                                                                      |
| ---------------------------------------------- | -------------------------------------------------------------------------------- |
| 법규 / `source_document`                       | 출처·문서 종류·발행 주체를 가진 문서. 개정본 하나를 뜻하지 않는다.               |
| 문서 버전 / `document_version`                 | 특정 원문과 공포·시행·수집 정보를 연결한 개정본.                                 |
| 조문 / `provision`                             | 조·항·호 등 원문 계층에 속한 규정 단위. 검색 청크와 구분한다.                    |
| 검색 청크 / `chunk`                            | 검색·입력 길이를 위해 나눈 단위. 원래 조문·버전·위치로 복원할 수 있어야 한다.    |
| 근거 / `evidence`                              | 주장에 연결하는, 특정 버전과 원문 위치가 확인된 자료.                            |
| 인용 / `citation`                              | 근거의 조문·버전·발췌·시행 정보·출처 URL을 전달하는 구조.                        |
| 주장 / `claim`                                 | 의무·허용·예외 등 근거로 뒷받침해야 하는 실질적인 설명.                          |
| 기준일 / `as_of_date`                          | 사용자가 법규 적용 여부를 확인하려는 날짜.                                       |
| 거래일 / `transaction_date`                    | 사례의 거래가 발생한 날짜. 기준일과 다르면 확인이 필요하다.                      |
| 공포일 / `promulgated_at`                      | 규정이 공포된 날짜. 시행일과 동일하다고 가정하지 않는다.                         |
| 시행일 / `effective_from`                      | 문서 버전의 시행 정보. 모든 조문의 적용 시작일로 복사하지 않는다.                |
| 적용 기간 / `valid_from`, `valid_to_exclusive` | 조문별로 검증한 시작일 포함·종료 경계 제외의 기간. 경과조치도 확인한다.          |
| 수집 시각 / `collected_at`                     | 원문을 확보한 시각. 법규의 시행 시점을 뜻하지 않는다.                            |
| 경과조치 / `transition_refs`                   | 개정 전후 적용에 영향을 주는 부칙 등의 참조. 날짜 필터만으로 해결하지 않는다.    |
| 말뭉치 스냅샷 / `corpus_snapshot`              | 검증한 문서 버전·지원 범위를 묶은 재현 가능한 데이터 단위.                       |
| 최신성 / `freshness_status`                    | 수집 성공 시각과 운영 설정에 따른 상태. 최신 법규 확인을 보증하지 않는다.        |
| 근거 확인                                      | 인용 원문과 버전을 대조했다는 표시. 법률 해석의 정확성 보증이 아니다.            |
| RAG                                            | 검색한 근거를 입력 문맥에 넣어 답변을 생성하는 구성.                             |
| SFT                                            | 검수한 입력·정답 데이터로 응답 행동을 학습하는 지도 미세조정.                    |
| LoRA / QLoRA                                   | SFT 등에 사용하는 효율적인 어댑터 학습 방법. SFT 다음의 별도 필수 단계가 아니다. |
| 답변 보류                                      | 근거·사실관계 부족으로 결론을 확정하지 않는 동작. 시스템 오류와 구분한다.        |

## 4. 데이터 모델과 불변 조건

### 4.1 PRD의 최소 엔터티

다음은 PRD 7.3절의 논리 모델이다. 실제 자료를 확인한 뒤 타입·인덱스·세부 제약을 확정하되, 출처·버전·시점·추적 관계를 잃지 않는다.

| 엔터티               | 주요 필드                                                                                                 | 지켜야 할 관계와 의미                                                                                |
| -------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `source_document`    | `source_id`, `official_id`, `title`, `document_type`, `authority`, `source_url`                           | `source_id`로 출처를 식별한다. 정확한 법규명·문서 성격·공식 URL은 수집 자료에서 얻는다.              |
| `document_version`   | `version_id`, `source_id`, `promulgated_at`, `effective_from`, `collected_at`, `content_hash`, `raw_path` | `source_id`는 출처를 참조한다. 원문을 보존하고 개정본을 덮어쓰지 않는다.                             |
| `provision`          | `provision_id`, `version_id`, `article`, `paragraph`, `item`, `text`, `source_offsets`, `parent_id`       | 버전과 원문 위치를 연결한다. 조·항·호와 부모 계층을 보존한다.                                        |
| `applicability`      | `provision_id`, `valid_from`, `valid_to_exclusive`, `transition_refs`, `temporal_review_status`           | 조문별 적용 기간·경과조치·검증 상태를 함께 확인한다. 종료 경계 누락을 무기한 유효로 해석하지 않는다. |
| `provision_relation` | `from_id`, `to_id`, `relation_type`, `verified`                                                           | 정의·위임·인용 관계와 확인 여부를 기록한다. 미확보 참조는 해결된 관계처럼 취급하지 않는다.           |
| `corpus_snapshot`    | `snapshot_id`, `version_ids`, `coverage`, `last_success_at`, `activated_at`                               | 사용 버전과 검증된 지원 법규·기간을 재현한다. 검증을 완료한 스냅샷만 활성화한다.                     |
| `model_release`      | `release_id`, `base_revision`, `adapter_id`, `training_dataset_id`, `eval_report_id`                      | 기반 모델·어댑터·학습 데이터·평가 보고서를 연결한다.                                                 |
| `query_trace`        | `request_id`, `as_of_date`, `snapshot_id`, `release_id`, `evidence_ids`, `status`, `latency_ms`           | 요청이 사용한 실제 데이터·모델·근거를 기록한다. 민감한 질문 원문은 기본 저장하지 않는다.             |

기본 관계는 `source_document → document_version → provision`이다. `applicability`는 조문을 참조하고, `provision_relation`은 조문 간 관계를 표현한다. 요청 기록은 사용한 스냅샷·모델 릴리스·근거를 연결한다.

### 4.2 추가 권장 설계: 저장·참조 규칙

- 날짜는 `YYYY-MM-DD` / DB `DATE`, 사건 시각은 시간대가 있는 timestamp로 구분한다. 시각은 UTC로 저장하고 한국 시간으로 표시하는 방식을 기본안으로 삼는다. 기준일 기본값은 `Asia/Seoul`의 오늘이다.
- 내부 ID는 변경되지 않는 불투명 식별자로 둔다. 같은 조문 번호라도 버전이 다르면 별도 `provision_id`로 구분한다. `evidence_ids`는 P0에서 버전이 고정된 `provision_id`를 가리키게 하고, 청크를 추가하면 조문으로의 매핑을 명시한다.
- 실제 외래키가 가리키는 레코드의 존재를 검증한다. `parent_id`는 같은 버전 안의 부모를 가리키고 순환하지 않게 한다. 문서 간 참조는 대상 조문의 버전까지 명시한다.
- 원문 바이트의 해시와 정규화 본문을 구분한다. `source_offsets`의 단위와 기준 원문을 명시하고, PDF·표처럼 문자 오프셋만으로 부족한 형식에는 페이지·셀 등 위치 정보를 추가한다.
- `document_type`의 초기 후보는 `law`, `enforcement_decree`, `supervisory_regulation`, `official_faq`, `official_interpretation`이다. 공식 분류와 매핑을 확인해 확정하고, 알 수 없는 분류를 법률로 추정하지 않는다.
- 부칙·별표를 일반 조 번호로 억지 변환하지 않는다. 필요하면 `provision_kind`, 별표 식별자와 구조화된 표 데이터를 추가해 단위·각주·조건을 보존한다.
- 미확보 참조를 표현하려면 `target_locator`, `resolution_status` 등을 추가하고 미해결 상태에서 `to_id`의 null을 허용하는 방식을 검토한다. 존재하지 않는 조문 ID를 만들어 외래키를 채우지 않는다.
- `temporal_review_status`는 `unreviewed`, `verified`, `needs_review`를 초기 후보로 둔다. 알 수 없는 날짜는 null과 사유로 남긴다. 적용 기간·검증 상태·스냅샷의 기간 범위를 함께 판정한다.
- 검색 인덱스에 `snapshot_id`와 빌드 설정을 연결한다. 요청 시작 시 사용할 스냅샷·모델 릴리스를 고정해 검색·생성·검증 사이의 버전 혼용을 막는다.
- 추론 전 장애에는 아직 선택하지 않은 모델·근거가 없을 수 있다. 이를 임의의 값으로 채우지 말고 추적 스키마에 null·빈 목록·실패 단계를 명시한다. 기본 모델 비교 실행의 `adapter_id`도 선택값으로 설계한다.

### 4.3 추가 권장 설계: 학습·평가 재현 모델

PRD 9절과 FR-12, NFR-01을 구현할 때 다음 논리 구조를 사용하거나 동등한 메타데이터를 보존한다. 각각을 별도 DB 테이블로 만들 필요는 없다.

| 구조               | 권장 필드                                                                                                                                                            | 목적                                                      |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| `dataset_manifest` | `dataset_id`, `version`, `content_hash`, `split_policy`, `group_ids`, `review_summary`                                                                               | 학습·개발·최종 평가 데이터 버전과 분리 기준 재현          |
| `training_example` | `example_id`, `dataset_id`, `group_id`, `split`, `question`, `as_of_date`, `facts`, `evidence_ids`, `expected_answer`, `expected_status`, `review_status`            | 사례·근거·기대 행동·검수 상태 연결                        |
| `evaluation_run`   | `run_id`, `experiment_id`, `release_id`, `dataset_id`, `snapshot_id`, `config_ref`, `metrics`, `failure_cases`, `artifact_path`                                      | E1~E4의 조건·결과·실패 사례 비교                          |
| `execution_config` | `config_id`, `tokenizer_revision`, `prompt_version`, `retrieval_config`, `index_version`, `generation_config`, `training_config`, `seed`, `quantization`, `hardware` | 요청·학습·평가 설정을 참조 가능한 파일 또는 레코드로 보존 |

`split` 후보는 `train`, `dev`, `test`다. 검수 상태는 PRD의 `draft`, `reviewed`, `needs_review`, `excluded`를 사용하며, 원문 대조를 완료한 `reviewed` 항목만 학습에 포함한다. 학습 예제의 질문 필드는 검수한 공개·가상 사례를 위한 것으로, 서비스의 사용자 질문 자동 저장을 허용하지 않는다.

## 5. 수집·시점·갱신 작업 절차

1. **출처 확인:** 공식 API의 이용 조건·응답 형식·버전 정보를 확인한다. 접근이 지연되면 공식 원문 파일을 수동 등록하고 URL·취득 시각·버전·해시를 기록한다.
2. **원문 보존:** 원본 파일·응답을 보관한 뒤 검색용 본문을 별도로 정규화한다. 정의·위임·인용·부칙·별표를 연결하고 미확보 항목을 표시한다.
3. **적용 범위 검증:** 조문별 시행 정보와 경과조치를 확인한다. 날짜 범위는 `valid_from <= as_of_date < valid_to_exclusive`로 해석하되, 종료 경계가 없거나 사실관계가 부족하면 다른 검증 정보 없이 적용을 확정하지 않는다.
4. **후보 스냅샷 검증:** 파싱·참조·시점·검색 인덱스를 검사하고 지원 법규·기간을 기록한다. 필요한 표·단위·조건이 추출되지 않은 내용에 의존하는 질문은 보류 대상으로 둔다.
5. **활성화·복구:** 검증을 완료한 새 스냅샷과 대응 인덱스를 함께 활성화한다. 실패하면 기존 스냅샷을 유지하고 갱신 상태를 표시한다. 직전 스냅샷·모델로 복구할 수 있게 한다.

P0는 관리자 수동 갱신을 지원한다. P1의 일일 변경 확인과 캐시에는 스냅샷 ID를 반영한다. 신선도 48시간은 잠정 제품 설정이며 법적 기한이 아니다. 마지막 시도 시각과 `last_success_at`을 혼동하지 않는다.

## 6. 질의 처리와 답변 계약

### 6.1 요청 처리 순서

1. 질문과 기준일을 읽는다. 기준일이 없으면 한국 시간 기준 오늘을 적용하고 화면에 표시한다. 질문에 별도 거래일이 있으면 사용할 날짜를 확인한다. (FR-01)
2. 지원 법규·검증 기간·필요 사실관계를 확인한다. 범위 밖 질문을 지원 가능으로 표시하지 않는다. (FR-02, FR-08)
3. 키워드·의미 검색을 결합하고 기준일·문서 종류를 반영한다. 법규명·조문 번호 직접 검색을 지원한다. 필요한 정의·위임·예외를 함께 조회하고 입력 길이 때문에 제외한 근거를 기록한다. (FR-03, FR-04)
4. 자체 모델에 근거와 사실관계를 제공하고 핵심 주장을 근거 ID에 연결해 생성한다. 공식 URL은 수집 메타데이터에서 가져온다. (FR-05, FR-09, FR-11)
5. 근거 ID 존재·지정 버전·발췌 일치·기준일 적합 여부를 검사한다. 실패한 인용과 그에 의존한 주장의 전달을 멈춘다. 확보한 근거로 **최대 1회 재생성**하고 다시 실패하면 해당 부분을 확인 보류로 전환한다. (FR-06, FR-07)
6. 확인된 내용 → 적용 조건 → 예외·추가 확인 사항 → 근거 → 기준일·수집 시점 순서로 표시한다. 상태와 제한 사항을 함께 전달하고 요청 추적 정보를 기록한다. (FR-13)

부분 답변이 가능해도 확인하지 못한 적용 결론을 전체 결론처럼 표시하지 않는다. 인용 검사 통과를 의미적 정확성 검증 완료로 표시하지 않는다.

### 6.2 상태 어휘

| `status`                | 사용할 상황                                                                    | 사용자에게 전달할 내용                |
| ----------------------- | ------------------------------------------------------------------------------ | ------------------------------------- |
| `answered`              | 확보한 근거로 검증을 통과한 설명을 제공할 수 있음                              | 설명·조건·예외·근거와 남은 한계       |
| `needs_clarification`   | 상품·거래일·당사자 등 추가 사실관계가 필요함                                   | 필요한 정보를 특정한 후속 질문        |
| `insufficient_evidence` | 정상 검색 후에도 근거 부족·자료 충돌·시점 불명확 등으로 결론을 뒷받침하지 못함 | 미확보 자료·불명확한 조건과 확인 보류 |
| `out_of_scope`          | 지원 법규·기간·업무 범위를 벗어남                                              | 현재 지원 범위와 해당 한계            |
| `system_error`          | 검색 서버·모델·검증 처리의 오류 또는 시간 초과                                 | 장애 상태와 가능한 원문 탐색 안내     |

### 6.3 API 경계

PRD 10.3절의 경로를 기본 계약으로 사용한다.

| API                                  | 역할                                                 |
| ------------------------------------ | ---------------------------------------------------- |
| `POST /v1/queries`                   | 질문·기준일·사실관계를 받아 답변 또는 후속 질문 반환 |
| `GET /v1/provisions/{provision_id}`  | 지정 버전 원문과 메타데이터 조회                     |
| `GET /v1/corpus/status`              | 지원 법규·기간·갱신 상태 조회                        |
| `POST /v1/admin/ingestions`          | 인증된 관리자 수집·검증 작업 시작                    |
| `GET /v1/admin/evaluations/{run_id}` | 관리자 평가 결과 조회                                |

질의 응답에는 `request_id`, `status`, `as_of_date`, `answer`, `claims[]`, `citations[]`, `conditions[]`, `follow_up_questions[]`, `limitations[]`, `corpus_snapshot_id`, `model_release_id`, `freshness_status`를 둔다. 보류 상태의 `answer`에는 확인되지 않은 확정 결론을 담지 않는다.

`claims[]`의 근거 ID는 `citations[]`에서 해석할 수 있어야 한다. 인용에는 조문·버전 ID, 법규명, 조·항·호, 원문 발췌, 시행 정보, 저장된 공식 URL을 포함한다. **추가 권장 설계:** API의 `corpus_snapshot_id` / `model_release_id`와 DB의 `snapshot_id` / `release_id` 매핑을 명시하고, 오류 응답에서 미확보 필드의 null·빈 목록 규칙을 정의한다.

## 7. 학습·평가 기준

- 데이터 작성·상태 전환·최종 평가 봉인에는 사용자가 확정한 [검수 정책](docs/review_policy.md)을 적용한다. 존재·의미·시점·완전성의 사람 검수 기록 없이 `draft`를 `reviewed`로 자동 승격하지 않는다. 개발자·외부 검토자의 이력과 공식 FAQ/해석자료 대조 여부를 구분한다.
- 기반 모델 실행 → 동일 모델의 RAG 기준선 → 근거 포함 SFT와 LoRA 또는 QLoRA → 통합 평가 순으로 진행한다. 외부 상용 LLM API를 사용자 답변의 필수 의존성으로 두지 않는다.
- 학습 데이터는 질문·기준일·사실관계·근거·답변·근거 ID·기대 상태를 포함한다. 자동 생성 문항은 검수 전까지 초안이다. 복잡한 해석을 확인하지 못했으면 정답을 임의로 확정하지 않는다.
- 동일 사례·패러프레이즈·관련 쟁점과 조문 묶음·사례 템플릿을 고려해 분할하고 유사 문항 누출을 확인한다. 최종 평가 질문·정답·해설은 학습·프롬프트 개선·검색 튜닝에 사용하지 않는다.
- 법규 원문은 검색 말뭉치에 넣을 수 있지만 평가 정답·해설은 넣지 않는다. 최종 평가 결과를 보고 개선하면 해당 세트를 개발 평가로 전환하고 새 최종 평가셋을 마련한다.
- 초기 20문항, 학습 300~500건, 개발 평가 50건, 최종 평가 100건은 제안 규모다. 최종 평가 유형은 단일 조문 30·다중 조문/예외 25·시점 15·정보 부족 15·범위 밖/근거 없음 15건을 기본안으로 삼는다.

| 실험 | 모델      | 검색 | 용도                    |
| ---- | --------- | ---- | ----------------------- |
| E1   | 기본 모델 | 없음 | 오프라인 진단           |
| E2   | 기본 모델 | 있음 | RAG 기준선              |
| E3   | SFT 모델  | 없음 | 오프라인 학습 효과 진단 |
| E4   | SFT 모델  | 있음 | 통합 후보 평가          |

E2와 E4의 검색 결과·입력 문맥·생성 한도·검증 규칙·기반 리비전·추론 양자화를 맞춘다. 원출력 오류와 검증 후 전달 오류, 답변 제공률과 불필요한 보류율을 따로 보고한다. 실제 법규 평가와 가상 규정 테스트 결과를 구분한다. 의미적 정합성은 사람이 검수하며 다른 LLM의 채점은 보조 수단으로 사용한다.

PRD 12절의 **잠정 내부 데모 목표**는 다음과 같다. 목표 변경은 사유·시점을 기록하고 최종 평가 전에 고정한다.

| 지표                  | 목표            | 해석 시 주의점                                         |
| --------------------- | --------------- | ------------------------------------------------------ |
| 근거 검색 Recall@5    | 90% 이상        | 답변 가능 질문별 정답 근거의 상위 5개 포함 비율을 평균 |
| 답변 정합성           | 85% 이상        | 답변 가능 문항 기준, 부당한 보류도 실패                |
| 주장별 근거 일치율    | 95% 이상        | 원문 존재 여부가 아닌 의미적 뒷받침 평가               |
| 허위·불일치 인용      | 평가셋 0건      | 최종 전달 답변 기준                                    |
| 시점 오류             | 평가셋 0건      | 검증한 시점 문항 기준                                  |
| 적절한 추가 질문·보류 | 90% 이상        | 정보 부족·범위 밖·근거 없음 문항 기준                  |
| 불필요한 보류         | 15% 이하        | 답변 가능 문항 기준                                    |
| 사용자 과업 성공      | 5개 중 4개 이상 | 질문 후 관련 근거를 열어 확인                          |

E4 채택에는 품질 기준 충족과 E2 대비 정합성·인용·시점 지표의 비악화가 필요하다. 전체 점수뿐 아니라 유형별 표본 수와 실패 사례를 기록한다. 점수 차이를 근거 없이 통계적 유의성으로 표현하지 않는다. 미세조정이 기준을 통과하지 못하면 실험 완료와 서비스 출시를 구분한다.

## 8. 금지사항

- 존재하지 않는 법규명·조문·버전·시행일·공식 URL을 생성하거나 예시 날짜를 실제 메타데이터로 등록하지 않는다.
- 현재 수집했다는 이유로 모든 과거 거래에 적용하거나, 시행 예정 규정을 현행 규정으로 제시하지 않는다. 검증하지 않은 기간을 지원 범위에 포함하지 않는다.
- 인용 검사 실패 후 인용만 삭제하고 법률상 결론을 그대로 전달하지 않는다. 실패한 생성을 제한 없이 반복하지 않는다.
- 조문의 단서·예외·부정 표현을 제거하거나 FAQ와 법령을 같은 효력으로 설명하지 않는다. 문서 종류의 순위만으로 자료 충돌의 법률 해석을 자동 확정하지 않는다.
- 검색 결과가 없다는 이유로 “관련 규정이 없다”고 단정하지 않는다. 장애를 근거 부족 상태로 숨기지 않는다.
- 보정되지 않은 “모델 신뢰도 95%”를 표시하거나 “근거 확인”을 법률 해석 보증으로 사용하지 않는다.
- 미검수 자동 생성 데이터를 검수 완료 데이터로 사용하지 않는다. 개발자 검수를 전문가 인증으로 표현하지 않는다.
- 최종 평가셋으로 학습·튜닝한 뒤 독립적인 최종 평가 결과라고 보고하지 않는다. 평가셋 오류 0건을 서비스 전체의 무오류 보장으로 확대하지 않는다.
- 실제 고객 정보·은행 내부 규정을 초기 공개 데이터·가상 사례 범위에 임의로 섞지 않는다. 사용자 질문 원문 저장을 기본 활성화하지 않는다.
- 검색 문서 안의 명령이나 사용자 입력이 시스템의 근거 검사·관리자 권한 통제를 바꾸게 하지 않는다.
- 검증하지 않은 스냅샷으로 정상 데이터를 덮어쓰거나 인증 없이 수집·모델 교체 관리 기능을 노출하지 않는다.
- 실제 고객 대상 금융 의사결정, 규제기관 자동 신고·제출, 전 금융 법규·전체 과거 시점 지원을 MVP에 포함된 기능처럼 구현·홍보하지 않는다.

## 9. 보안·운영 기준

- 관리자 수집·모델 교체 기능에는 인증을 적용하고, 외부 공개 시 요청 제한과 접근 범위를 설정한다. 실제 연동 코드·설정에는 비밀값을 직접 넣지 않는 방식을 추가 권장한다.
- 요청 추적은 버전·근거·상태·지연 시간 중심으로 남긴다. 평가용 사용자 입력 수집은 마스킹·선별 후 별도로 관리한다. 오류 로그에도 원문 질문이 자동으로 유출되지 않도록 확인한다.
- 추적 기록 7일 보관, 신선도 48시간, 단일 동시 요청 p95 30초는 잠정 운영 목표다. 실제 목적·데이터·장비 측정에 따라 확정하며 법정 의무나 달성 성과로 표현하지 않는다.
- 검색·추론·검증 지연을 구분하고 토큰 수·장비·양자화 설정·GPU 시간·저장량을 함께 기록한다.
- 수집·모델 장애 시 가능한 원문 탐색을 제공하고 직전 데이터·모델로 복구할 수 있어야 한다. **추가 권장 설계:** 답변 캐시를 도입하면 스냅샷 외에도 기준일·사실관계·모델·프롬프트 설정 차이가 결과에 반영되도록 키를 설계한다.

## 10. 개발 작업 절차와 완료 기준

작업 시작 시 관련 PRD 절과 FR/NFR ID, 기존 코드·데이터 형식·테스트를 확인한다. 아직 구현이 없으면 가정과 미확정 사항을 구분한 최소 구현부터 시작한다. 장비·예산·초기 업무 범위·정답 검수 방식이 미확정이어도 원문 목록과 가상 예시 질문 설계는 진행할 수 있다.

변경 시 데이터 → 검색 → 생성 → 검증 → 표시·추적 중 영향을 받는 경로를 확인한다. 해당 변경에 필요한 검증을 수행하고 결과·미검증 사항·관련 요구사항을 보고한다. 문서만 바꾸는 작업에 모델 재학습이나 전체 평가를 요구하지 않는다.

구현 변경의 검증은 실제 영향을 받는 실패 사례를 중심으로 선택한다.

| 변경 영역        | 우선 확인할 사례                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------- |
| 파싱·청킹·스키마 | 단서·부정·표 단위 보존, 부모·버전 참조, 원문 발췌 복원, 미확보 참조                               |
| 시점·검색        | 적용 시작일과 종료 경계, 시행 예정 규정, 경과조치, 미검증 기간, 법규명·조문 번호 직접 검색        |
| 생성·검증·응답   | 허위 ID·틀린 버전·불일치 발췌·생성 URL 차단, 최대 1회 재생성, 실패 주장 보류, 상태 구분           |
| 수집·운영        | 갱신 실패 시 기존 스냅샷 유지, 요청 중 버전 일관성, 인증, 질문 원문 비저장, 장애·시간 초과와 복구 |
| 학습·평가        | 그룹 분할·유사 문항 누출, 어댑터 저장·재로딩, E2/E4 동일 조건, 불필요한 보류, 의미적 근거 불일치  |

MVP 완료 시에는 PRD 12·14절을 대조해 다음 결과물을 확인한다.

- 지원 범위·공식 원문·버전·시행 정보가 추적되는 말뭉치와 검색 인덱스.
- 기반 모델과 학습 어댑터를 재로딩할 수 있는 자체 추론 환경.
- 질문·기준일 입력부터 답변·근거 원문 확인 또는 적절한 보류까지의 사용자 흐름.
- 분리·검수한 학습·개발·최종 평가 데이터, E1~E4 보고서, 실패 사례와 모델 채택 판단.
- 범위 밖·시점 불명확·자료 충돌·장애를 구분하는 처리와 갱신·복구 기록.
- 출처·라이선스·버전·실행 설정·장비·비용 기록, 실행 안내·API 명세·데이터 카드·모델 카드·시연 자료.

법규 최신성·개별 API 사용법·모델 라이선스처럼 외부 확인이 필요한 작업은 해당 시점의 공식 원문을 확인하고 확인 일시를 기록한다. 확인하지 못한 항목은 미확인으로 남기고, PRD나 이 문서에 적혀 있다는 이유만으로 확인 완료 처리하지 않는다.
